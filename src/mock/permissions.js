import { DEFAULT_PERMISSION_MATRIX, ENTERPRISE_MODULES } from '@/lib/admin/permissionLogic'

export const permissionModules = ENTERPRISE_MODULES
export const permissionMatrix = DEFAULT_PERMISSION_MATRIX
